import React from 'react'
import { Container } from '@material-ui/core'

const Add = (props) => {
    return(
        <Container maxwidth="md">
            <h1>Log in and you can add things here.</h1>
        </Container>

    )
}

export default Add;